#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * puts2 - Returns other strings not all
 * @str: string chars tested
 *
 */

void puts2(char *str)
{
	int i;

	for (i = 0; i < strlen(s); i = i + 2)
	{
		printf("%c", s[i]);
	}
	printf("\n");
}
