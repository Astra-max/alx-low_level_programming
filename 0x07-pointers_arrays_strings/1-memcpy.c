#include "main.h"
/**
* _memcpy - copies mem addres
* @dest: char tested
* @src: char tested
* @n: integer
* Return: Dest
*/

char *_memcpy(char *dest, char *src, unsigned int n)
{
int = r;
int i = n;

for (; r < i; r++)
{
dest[r] = src[r];
n--;
}
return (dest);
}
